import React from "react";

// Styles

import "./TopBar.css";

const TopBar = () => {
  return (
    <header className="container-topbar">
      <h1>TodoApp</h1>
    </header>
  );
};

export default TopBar;
